#include <bits/stdc++.h>
using namespace std;

// Approach 
// Reverse the given string S and store it in S'.
// Find the longest common substring between S and S'.

// Time Complexity - O(n ^ 2), where n = size of S or S'
// Space Complexity - O(n ^ 2), this can be optimised to O(n)

