#include <bits/stdc++.h>
using namespace std;

int main() {
    // Array of vector
    vector<int> adj[3];
    // adj[i].push_back(ele);

    // array of 2 D vector
    vector<vector<int>> adj[3];
    // adj[i].push_back({ele, ele, ...});
}